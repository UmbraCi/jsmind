# jsMind、Mind-Elixir-Core 与 Simple-Mind-Map 插件机制深度对比分析

## 目录

1. [概述](#概述)
2. [jsMind 插件机制](#jsmind-插件机制)
3. [Mind-Elixir-Core 插件机制](#mind-elixir-core-插件机制)
4. [Simple-Mind-Map 插件机制](#simple-mind-map-插件机制)
5. [三者详细对比](#三者详细对比)
6. [优劣势分析](#优劣势分析)
7. [实践建议](#实践建议)

---

## 概述

jsMind、Mind-Elixir-Core 和 Simple-Mind-Map 是三个流行的 Web 思维导图库，它们采用了不同的插件机制来扩展功能。本文档基于源码分析，详细对比三者的插件机制实现，分析各自的优劣势，并提供实践建议。

### 核心差异速览

| 特性               | jsMind             | Mind-Elixir-Core    | Simple-Mind-Map     |
| ------------------ | ------------------ | ------------------- | ------------------- |
| **设计模式**       | 类 + 注册机制      | 函数式              | 类 + 静态方法       |
| **初始化时机**     | 异步（setTimeout） | 同步（init 方法内） | 同步（构造函数内）  |
| **生命周期管理**   | 无统一管理         | disposable 数组     | 生命周期钩子        |
| **第三方插件支持** | ✅ 支持            | ❌ 不支持           | ✅ 支持             |
| **配置方式**       | plugin 配置对象    | Options 配置项      | usePlugin 静态方法  |
| **插件移除**       | ❌ 不支持          | ✅ 支持             | ✅ 支持             |
| **插件预加载**     | ❌ 不支持          | ❌ 不支持           | ✅ 支持（preload）  |
| **插件继承**       | ✅ 支持            | ❌ 不支持           | ✅ 支持             |

---

## jsMind 插件机制

### 1. 插件定义

jsMind 的插件是一个**类**，需要实现特定的接口：

```javascript
// src/plugins/jsmind.multiline-text.js
export class MultilineText {
    constructor(jm, options) {
        this.jm = jm;           // jsMind 实例
        this.options = options;  // 插件配置
        this.editing_node = null;
        this.multiline_editor = null;
    }

    init() {
        // 插件初始化逻辑
        const view = this.jm.view;
        
        // 覆盖编辑方法
        view.edit_node_begin = this.edit_node_begin.bind(this);
        view.edit_node_end = this.edit_node_end.bind(this);
        
        // 重新渲染所有节点
        this._rerender_all_nodes();
    }

    edit_node_begin(node) {
        // 编辑开始逻辑
    }

    edit_node_end() {
        // 编辑结束逻辑
    }
}
```

### 2. 插件注册

使用 `jsMind.plugin` 类创建插件实例，然后通过 `register_plugin()` 注册：

```javascript
// 创建插件实例
export const multiline_text_plugin = new jsMind.plugin(
    'multiline_text',  // 插件名称
    function (jm, options) {
        const mt = new MultilineText(jm, options);
        mt.init();
        jm.multiline_text = mt;  // 将插件实例挂载到 jsMind 实例上
    }
);

// 注册插件
jsMind.register_plugin(multiline_text_plugin);
```

### 3. 插件初始化

插件在 `jsmind.plugin.js` 中**异步**初始化：

```javascript
// src/jsmind.plugin.js
export function apply(jm, options) {
    $.w.setTimeout(function () {
        _apply(jm, options);
    }, 0);  // ← 异步初始化
}

function _apply(jm, options) {
    w.plugins.forEach(plugin => plugin.fn_init(jm, options[plugin.name]));
}
```

**时序**：
```
1. jsMind 实例创建
2. 调用 show() 方法
3. 数据加载、布局计算、节点渲染
4. 异步调用插件初始化（setTimeout）
5. 插件初始化完成
```

### 4. 插件配置

通过 `plugin` 配置对象传递插件选项：

```javascript
const jm = new jsMind({
    container: 'jsmind_container',
    editable: true,
    plugin: {
        multiline_text: {
            text_width: 200,
            editor_border_color: '#4CAF50',
            auto_resize: true,
        }
    }
});
```

### 5. 特点总结

**优势**：
- ✅ 支持第三方插件动态注册
- ✅ 插件隔离，命名空间清晰
- ✅ 灵活性高，可运行时加载

**劣势**：
- ❌ 异步初始化导致无法影响初始渲染
- ❌ 无生命周期管理，容易内存泄漏
- ❌ UMD 模块作用域隔离问题

---

## Mind-Elixir-Core 插件机制

### 1. 插件定义

Mind-Elixir-Core 的插件是一个**函数**，返回清理函数：

```typescript
// src/plugin/nodeDraggable.ts
export default function (mind: MindElixirInstance) {
    // 1. 插件状态（闭包）
    let insertType: InsertType = null;
    let meet: Topic | null = null;
    const ghost = createGhost(mind);
    const edgeMoveController = new EdgeMoveController(mind);

    // 2. 事件处理函数
    const handleDragStart = (e: DragEvent) => {
        mind.selection.cancel();
        const target = e.target as Topic;
        // ... 拖拽开始逻辑
    };

    const handleDragEnd = (e: DragEvent) => {
        // ... 拖拽结束逻辑
    };

    const handleDragOver = (e: DragEvent) => {
        // ... 拖拽过程逻辑
    };

    // 3. 注册事件监听
    const off = on([
        { dom: mind.map, evt: 'dragstart', func: handleDragStart },
        { dom: mind.map, evt: 'dragend', func: handleDragEnd },
        { dom: mind.map, evt: 'dragover', func: handleDragOver },
    ]);

    // 4. 返回清理函数
    return off;
}
```

### 2. 插件导入

直接在 `methods.ts` 中导入插件：

```typescript
// src/methods.ts
import contextMenu from './plugin/contextMenu';
import keypressInit from './plugin/keypress';
import nodeDraggable from './plugin/nodeDraggable';
import operationHistory from './plugin/operationHistory';
import toolBar from './plugin/toolBar';
import selection from './plugin/selection';
```

### 3. 插件初始化

插件在 `init()` 方法中**同步**调用：

```typescript
// src/methods.ts
export default {
    init(this: MindElixirInstance, data: MindElixirData) {
        // 数据初始化
        this.nodeData = data.nodeData;
        fillParent(this.nodeData);
        
        // 插件初始化（同步）
        this.toolBar && toolBar(this);
        
        if (import.meta.env.MODE !== 'lite') {
            this.keypress && keypressInit(this, this.keypress);
            
            if (this.editable) {
                selection(this);
            }
            
            if (this.contextMenu) {
                this.disposable.push(contextMenu(this, this.contextMenu));
            }
            
            this.draggable && this.disposable.push(nodeDraggable(this));
            this.allowUndo && this.disposable.push(operationHistory(this));
        }
        
        // 布局和渲染
        this.layout();
        this.linkDiv();
        this.toCenter();
    }
}
```

### 4. 插件清理

在 `destroy()` 方法中调用所有清理函数：

```typescript
// src/methods.ts
destroy(this: Partial<MindElixirInstance>) {
    // 调用所有清理函数
    this.disposable!.forEach(fn => fn());
    
    // 清理实例属性
    if (this.el) this.el.innerHTML = '';
    this.el = undefined;
    this.nodeData = undefined;
    // ...
}
```

### 5. 特点总结

**优势**：
- ✅ 同步初始化，可影响初始渲染
- ✅ 统一生命周期管理（disposable 数组）
- ✅ 函数式设计，简洁直观
- ✅ TypeScript 类型安全

**劣势**：
- ❌ 不支持第三方插件
- ❌ 编译时确定，无法动态加载
- ❌ 缺乏插件隔离

---

## Simple-Mind-Map 插件机制

### 1. 插件定义

Simple-Mind-Map 的插件是一个**类**，需要实现特定的接口和静态属性：

```javascript
// simple-mind-map/src/plugins/Drag.js
class Drag extends Base {
    // 构造函数
    constructor({ mindMap }) {
        super(mindMap.renderer);
        this.mindMap = mindMap;
        this.autoMove = new AutoMove(mindMap);
        this.reset();
        this.bindEvent();
    }

    // 复位
    reset() {
        this.isDragging = false;
        this.mousedownNode = null;
        this.beingDragNodeList = [];
        // ...
    }

    // 绑定事件
    bindEvent() {
        this.onNodeMousedown = this.onNodeMousedown.bind(this);
        this.onMousemove = this.onMousemove.bind(this);
        this.onMouseup = this.onMouseup.bind(this);
        
        this.mindMap.on('node_mousedown', this.onNodeMousedown);
        this.mindMap.on('mousemove', this.onMousemove);
        this.mindMap.on('node_mouseup', this.onMouseup);
        this.mindMap.on('mouseup', this.onMouseup);
    }

    // 解绑事件
    unBindEvent() {
        this.mindMap.off('node_mousedown', this.onNodeMousedown);
        this.mindMap.off('mousemove', this.onMousemove);
        this.mindMap.off('node_mouseup', this.onMouseup);
        this.mindMap.off('mouseup', this.onMouseup);
    }

    // 插件被移除前的生命周期钩子
    beforePluginRemove() {
        this.unBindEvent();
    }

    // 插件被销毁前的生命周期钩子
    beforePluginDestroy() {
        this.unBindEvent();
    }
}

// 静态属性：插件实例名称
Drag.instanceName = 'drag';

export default Drag;
```

### 2. 插件注册

Simple-Mind-Map 使用**静态方法 `usePlugin()`** 注册插件：

```javascript
import MindMap from 'simple-mind-map';
import Drag from 'simple-mind-map/src/plugins/Drag.js';

// 注册插件
MindMap.usePlugin(Drag);

// 创建实例
const mindMap = new MindMap({
    el: document.getElementById('mindMapContainer'),
    data: {
        data: { text: '根节点' },
        children: []
    }
});
```

**静态方法实现**：

```javascript
// simple-mind-map/index.js
MindMap.pluginList = [];

MindMap.usePlugin = (plugin, opt = {}) => {
    if (MindMap.hasPlugin(plugin) !== -1) return MindMap;
    plugin.pluginOpt = opt;
    MindMap.pluginList.push(plugin);
    return MindMap;
};

MindMap.hasPlugin = plugin => {
    return MindMap.pluginList.findIndex(item => {
        return item === plugin;
    });
};
```

### 3. 插件初始化

插件在 MindMap 构造函数中**同步**初始化，支持**预加载**：

```javascript
// simple-mind-map/index.js
class MindMap {
    constructor(opt = {}) {
        // ... 初始化容器、主题、缓存
        
        // 1. 注册预加载插件（在核心模块之前）
        MindMap.pluginList
            .filter(plugin => plugin.preload)
            .forEach(plugin => {
                this.initPlugin(plugin);
            });

        // 2. 初始化核心模块
        this.event = new Event({ mindMap: this });
        this.keyCommand = new KeyCommand({ mindMap: this });
        this.command = new Command({ mindMap: this });
        this.renderer = new Render({ mindMap: this });
        this.view = new View({ mindMap: this });

        // 3. 注册普通插件（在核心模块之后）
        MindMap.pluginList
            .filter(plugin => !plugin.preload)
            .forEach(plugin => {
                this.initPlugin(plugin);
            });

        // 4. 初始渲染
        this.render(this.opt.fit ? () => this.view.fit() : () => {});
    }

    // 实例化插件
    initPlugin(plugin) {
        if (this[plugin.instanceName]) return;
        this[plugin.instanceName] = new plugin({
            mindMap: this,
            pluginOpt: plugin.pluginOpt
        });
    }
}
```

**时序**：
```
1. MindMap 实例创建
2. 初始化容器、主题、缓存
3. 初始化预加载插件（preload: true）
4. 初始化核心模块（Event、KeyCommand、Command、Renderer、View）
5. 初始化普通插件（preload: false）
6. 渲染思维导图
```

### 4. 插件移除

Simple-Mind-Map 支持动态移除插件：

```javascript
// 移除插件
mindMap.removePlugin(Drag);

// 内部实现
removePlugin(plugin) {
    let index = MindMap.hasPlugin(plugin);
    if (index !== -1) {
        MindMap.pluginList.splice(index, 1);
        if (this[plugin.instanceName]) {
            // 调用生命周期钩子
            if (this[plugin.instanceName].beforePluginRemove) {
                this[plugin.instanceName].beforePluginRemove();
            }
            delete this[plugin.instanceName];
        }
    }
}
```

### 5. 插件销毁

在销毁思维导图时，会调用所有插件的销毁钩子：

```javascript
// 销毁思维导图
mindMap.destroy();

// 内部实现
destroy() {
    this.emit('beforeDestroy');
    // 清除节点编辑框
    this.renderer.textEdit.hideEditTextBox();
    this.renderer.textEdit.removeTextEditEl();
    
    // 移除插件
    [...MindMap.pluginList].forEach(plugin => {
        if (
            this[plugin.instanceName] &&
            this[plugin.instanceName].beforePluginDestroy
        ) {
            this[plugin.instanceName].beforePluginDestroy();
        }
        this[plugin.instanceName] = null;
    });
    
    // 解绑事件
    this.event.unbind();
    // 移除画布节点
    this.svg.remove();
    // ...
}
```

### 6. 插件预加载

Simple-Mind-Map 支持插件预加载，允许插件在核心模块之前初始化：

```javascript
class YourPlugin {
    constructor({ mindMap }) {
        this.mindMap = mindMap;
    }
}

YourPlugin.instanceName = 'yourPlugin';
YourPlugin.preload = true;  // 设置为预加载插件

export default YourPlugin;
```

### 7. 插件继承

Simple-Mind-Map 支持插件继承：

```javascript
import Drag from 'simple-mind-map/src/plugins/Drag.js';

class CustomDrag extends Drag {
    constructor(opt) {
        super(opt);
        // 扩展功能
    }
    
    // 重写方法
    onNodeMousedown(node, e) {
        super.onNodeMousedown(node, e);
        // 添加额外逻辑
    }
}

CustomDrag.instanceName = 'customDrag';

export default CustomDrag;
```

### 8. 特点总结

**优势**：
- ✅ 同步初始化，可影响初始渲染
- ✅ 支持第三方插件
- ✅ 完善的生命周期管理（beforePluginRemove、beforePluginDestroy）
- ✅ 支持插件预加载（preload）
- ✅ 支持插件继承
- ✅ 支持动态移除插件

**劣势**：
- ❌ 插件必须通过静态方法注册，不如 jsMind 灵活
- ❌ 插件实例直接挂载到 MindMap 实例上，可能存在命名冲突

---

## 三者详细对比

### 1. 插件定义方式

| 特性         | jsMind      | Mind-Elixir-Core | Simple-Mind-Map |
| ------------ | ----------- | ---------------- | --------------- |
| **形式**     | 类（Class） | 函数（Function） | 类（Class）     |
| **状态管理** | 实例属性    | 闭包变量         | 实例属性        |
| **方法定义** | 类方法      | 闭包函数         | 类方法          |
| **清理机制** | 无          | 返回清理函数     | 生命周期钩子    |
| **静态属性** | 无          | 无               | instanceName    |

### 2. 插件注册方式

| 特性           | jsMind                  | Mind-Elixir-Core | Simple-Mind-Map         |
| -------------- | ----------------------- | ---------------- | ----------------------- |
| **注册方式**   | register_plugin()       | 直接导入         | usePlugin()             |
| **注册时机**   | 任意时机                | 编译时           | 实例化前                |
| **动态注册**   | ✅ 支持                 | ❌ 不支持        | ✅ 支持                 |
| **第三方插件** | ✅ 支持                 | ❌ 不支持        | ✅ 支持                 |
| **插件配置**   | plugin 对象             | Options 配置项   | usePlugin 第二个参数    |

### 3. 初始化时机

| 特性           | jsMind                  | Mind-Elixir-Core        | Simple-Mind-Map         |
| -------------- | ----------------------- | ----------------------- | ----------------------- |
| **时机**       | 异步（setTimeout）      | 同步（init 方法内）     | 同步（构造函数内）      |
| **顺序**       | 渲染后                  | 渲染前                  | 渲染前                  |
| **影响初始渲染** | ❌ 不能                 | ✅ 可以                 | ✅ 可以                 |
| **预加载支持** | ❌ 不支持               | ❌ 不支持               | ✅ 支持（preload）      |

**时序对比**：

```
jsMind:
1. 实例创建
2. show() → 数据加载 → 布局 → 渲染
3. setTimeout → 插件初始化
4. 插件需要重新渲染才能生效

Mind-Elixir-Core:
1. 实例创建
2. init() → 数据加载 → 插件初始化 → 布局 → 渲染
3. 插件直接影响初始渲染

Simple-Mind-Map:
1. 实例创建
2. 初始化容器、主题、缓存
3. 预加载插件初始化（preload: true）
4. 核心模块初始化
5. 普通插件初始化（preload: false）
6. 渲染思维导图
```

### 4. 生命周期管理

| 特性           | jsMind      | Mind-Elixir-Core        | Simple-Mind-Map                     |
| -------------- | ----------- | ----------------------- | ----------------------------------- |
| **清理机制**   | 无          | disposable 数组         | 生命周期钩子                        |
| **移除支持**   | ❌ 不支持   | ✅ 支持                 | ✅ 支持                             |
| **销毁钩子**   | 无          | destroy() 调用清理函数  | beforePluginDestroy()               |
| **移除钩子**   | 无          | 无                      | beforePluginRemove()                |
| **资源管理**   | 手动        | 自动                    | 自动                                |

### 5. 插件扩展性

| 特性           | jsMind      | Mind-Elixir-Core | Simple-Mind-Map |
| -------------- | ----------- | ---------------- | --------------- |
| **插件继承**   | ✅ 支持     | ❌ 不支持        | ✅ 支持         |
| **插件组合**   | ✅ 支持     | ✅ 支持          | ✅ 支持         |
| **插件通信**   | 通过实例    | 通过实例         | 通过实例        |
| **插件隔离**   | 命名空间    | 无               | 命名空间        |

### 6. 配置方式

**jsMind**：
```javascript
new jsMind({
    plugin: {
        multiline_text: {
            text_width: 200,
        }
    }
});
```

**Mind-Elixir-Core**：
```typescript
new MindElixir({
    draggable: true,
    contextMenu: {
        // 配置项
    },
});
```

**Simple-Mind-Map**：
```javascript
MindMap.usePlugin(Drag, {
    // 插件配置
});

new MindMap({
    el: '#container',
    data: {}
});
```

---

## 优劣势分析

### jsMind 插件机制

#### 优势

1. **第三方插件支持** ✅
   - 通过 `register_plugin()` 可以动态注册第三方插件
   - 插件可以独立开发和分发
   - 支持插件生态系统

2. **插件隔离** ✅
   - 每个插件是独立的类实例
   - 插件之间不会相互影响
   - 命名空间清晰（挂载到 jsMind 实例上）

3. **灵活性** ✅
   - 可以在运行时动态加载插件
   - 可以根据需要启用/禁用插件
   - 插件可以访问 jsMind 的所有 API

#### 劣势

1. **异步初始化问题** ❌
   - 插件在渲染后才初始化
   - 无法直接影响初始渲染
   - 需要重新渲染才能生效（性能开销）

2. **无生命周期管理** ❌
   - 没有统一的清理机制
   - 容易造成内存泄漏
   - 插件需要自己管理资源

3. **模块作用域问题** ❌
   - UMD 模块的作用域隔离
   - 插件无法直接访问内部类（如 ViewProvider）
   - 需要通过实例的 `constructor` 获取类

### Mind-Elixir-Core 插件机制

#### 优势

1. **同步初始化** ✅
   - 插件在渲染前初始化
   - 可以直接影响初始渲染
   - 无异步延迟

2. **统一生命周期管理** ✅
   - 通过 `disposable` 数组统一管理
   - 自动清理资源
   - 避免内存泄漏

3. **简洁性** ✅
   - 函数式设计，简单直观
   - 无需注册，直接导入
   - 无需类，避免复杂性

4. **类型安全** ✅
   - TypeScript 提供完整的类型检查
   - 编译时发现错误
   - IDE 智能提示

#### 劣势

1. **不支持第三方插件** ❌
   - 所有插件都是内置的
   - 无法动态添加第三方插件
   - 需要修改源码才能添加新插件

2. **编译时确定** ❌
   - 插件在编译时就确定了
   - 无法运行时动态加载
   - 不利于插件生态系统

3. **缺乏隔离** ❌
   - 插件可以直接修改实例的任何属性
   - 插件之间可能存在命名冲突
   - 缺乏命名空间

### Simple-Mind-Map 插件机制

#### 优势

1. **同步初始化** ✅
   - 插件在渲染前初始化
   - 可以直接影响初始渲染
   - 无异步延迟

2. **完善的生命周期管理** ✅
   - `beforePluginRemove()` 钩子
   - `beforePluginDestroy()` 钩子
   - 自动清理资源

3. **支持第三方插件** ✅
   - 通过 `usePlugin()` 注册第三方插件
   - 插件可以独立开发和分发
   - 支持插件生态系统

4. **插件预加载** ✅
   - 支持 `preload` 属性
   - 插件可以在核心模块之前初始化
   - 灵活控制初始化顺序

5. **插件继承** ✅
   - 支持插件继承
   - 可以扩展现有插件
   - 代码复用

6. **动态移除插件** ✅
   - 支持 `removePlugin()` 方法
   - 调用生命周期钩子
   - 清理插件资源

#### 劣势

1. **静态方法注册** ❌
   - 插件必须通过 `usePlugin()` 注册
   - 不如 jsMind 的 `register_plugin()` 灵活
   - 需要在实例化前注册

2. **命名冲突风险** ❌
   - 插件实例直接挂载到 MindMap 实例上
   - 可能与实例属性冲突
   - 需要通过 `instanceName` 避免冲突

---

## 实践建议

### 选择 jsMind 插件机制的场景

1. **需要支持第三方插件**
   - 开源项目，希望社区贡献插件
   - 需要插件生态系统
   - 需要动态加载插件

2. **需要插件隔离**
   - 多个插件可能存在命名冲突
   - 需要独立开发和测试插件
   - 需要插件版本管理

3. **需要运行时控制**
   - 需要根据用户配置动态启用/禁用插件
   - 需要在运行时加载插件
   - 需要插件热更新

### 选择 Mind-Elixir-Core 插件机制的场景

1. **内置插件为主**
   - 所有插件都是内置的
   - 不需要第三方插件支持
   - 追求简洁性和性能

2. **需要严格控制插件质量**
   - 所有插件都经过严格测试
   - 需要统一的代码风格
   - 需要类型安全

3. **需要同步初始化**
   - 插件需要影响初始渲染
   - 不能接受异步延迟
   - 需要最佳性能

### 选择 Simple-Mind-Map 插件机制的场景

1. **需要完善的插件系统**
   - 需要支持第三方插件
   - 需要完善的生命周期管理
   - 需要插件预加载

2. **需要插件继承**
   - 需要扩展现有插件
   - 需要代码复用
   - 需要插件组合

3. **需要动态管理插件**
   - 需要动态移除插件
   - 需要插件热更新
   - 需要插件版本管理

### 混合方案

对于 jsMind 的多行文本插件，我们采用了混合方案：

1. **配置式插件**（类似 Mind-Elixir-Core）
   - 通过 `custom_node_render` 配置项扩展功能
   - 避免异步初始化的时序问题

2. **重新渲染**
   - 插件初始化后重新渲染所有节点
   - 确保插件生效

3. **批量更新**
   - 避免多次触发重排重绘
   - 提升性能

---

## 总结

| 特性                 | jsMind | Mind-Elixir-Core | Simple-Mind-Map | 推荐场景                     |
| -------------------- | ------ | ---------------- | --------------- | ---------------------------- |
| **设计模式**         | 类     | 函数式           | 类              | 根据项目需求                 |
| **第三方插件**       | ✅     | ❌               | ✅              | 需要插件生态选 jsMind/Simple |
| **初始化时机**       | 异步   | 同步             | 同步            | 需要影响初始渲染选后两者     |
| **生命周期管理**     | ❌     | ✅               | ✅              | 需要资源管理选后两者         |
| **插件预加载**       | ❌     | ❌               | ✅              | 需要控制初始化顺序选 Simple  |
| **插件继承**         | ✅     | ❌               | ✅              | 需要代码复用选 jsMind/Simple |
| **简洁性**           | 中     | 高               | 中              | 追求简洁选 Mind-Elixir       |
| **灵活性**           | 高     | 中               | 高              | 需要灵活性选 jsMind/Simple   |

### 最终建议

1. **如果需要支持第三方插件和插件生态系统**，选择 **jsMind** 或 **Simple-Mind-Map** 的插件机制
2. **如果追求简洁性、性能和类型安全**，选择 **Mind-Elixir-Core** 的插件机制
3. **如果需要完善的插件系统（生命周期、预加载、继承）**，选择 **Simple-Mind-Map** 的插件机制
4. **如果需要两者的优点**，可以采用**混合方案**（配置式插件 + 重新渲染）

### 核心差异

- **jsMind**：异步初始化，支持第三方插件，但无生命周期管理
- **Mind-Elixir-Core**：同步初始化，函数式设计，但不支持第三方插件
- **Simple-Mind-Map**：同步初始化，支持第三方插件，完善的生命周期管理，支持预加载和继承

**Simple-Mind-Map 的插件机制是三者中最完善的**，它结合了 jsMind 的第三方插件支持和 Mind-Elixir-Core 的同步初始化优势，同时提供了完善的生命周期管理、插件预加载和插件继承功能。

